
@model IEnumerable<Statut>

@{
    ViewData["Title"] = "Liste des preparation auto ";
}




<div class="row">


    <div class="col-md-8 offset-md-2">

        <div class="text-white">

            <a class="btn btn-outline-info  " asp-controller="Statut" asp-action="Create">

                <i class="bi bi-patch-plus"></i>  ajouter une préparation Auto
            </a>

        </div>
        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>@Html.DisplayNameFor(model => model.StatutVehicule)</th>
                    <th>@Html.DisplayNameFor(model => model.Vehicule.MarqueVehicule)</th>
                    


                    <th>Actions</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.StatutVehicule)
                        </td>

                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.Vehicule.MarqueVehicule)
                        </td>
                        







                        <td class="align-middle">

                            <a class="btn btn-outline-primary  " asp-action="Edit" asp-route-id="@item.Id">        <i class="bi bi-pencil-square">  </i>  Modifier</a> |


                            @Html.ActionLink("Supprimer", "delete", new { id = @item.Id }, new { @class = "btn btn-danger" })



                        </td>


                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>

